#include <iostream>;
#include <vector>;
using namespace std;
class fVector
{
    std::vector<float> v;
public:
    fVector(int size)
    {
        v = vector<float>(size);
    }
};

main(


)